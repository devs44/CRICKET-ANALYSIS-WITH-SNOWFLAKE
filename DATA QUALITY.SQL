SELECT * FROM CRICKET.CLEAN.MATCH_DETAIL_CLEAN 
WHERE MATCH_TYPE_NUMBER=4671;

SELECT
    TEAM_NAME,
    SUM(RUNS) + SUM(EXTRA_RUNS)
FROM
    CRICKET.CLEAN.DELIVERY_CLEAN_TBL
WHERE MATCH_TYPE_NUMBER = 4671
GROUP BY TEAM_NAME, BATTER
ORDER BY 1,2 DESC;
